//! Stub implementations of application ports.
//!
//! Populated incrementally as ports are defined under
//! `src/application/ports/`. Each stub loads canned responses through
//! [`super::fixture_loader`].

// Intentionally empty during the scaffolding phase. First stubs land with
// the Home use cases (see `plan/1-home.md`).
